# Loads Io's loopback script (Valve's CreateLoopback, see the script header).
wireplumber.components = [
  { name = io-create-loopback.lua, type = script/lua, provides = custom.io-loopback }
]

wireplumber.profiles = {
  main = {
    custom.io-loopback = required
  }
}
